/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', // এই লাইন এখানে রাখতে হবে
  // তোমার অন্য কনফিগারেশনগুলো এখানে থাকবে
}

module.exports = nextConfig;